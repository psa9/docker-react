language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t psa39/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true psa39/docker-react npm run test -- --coverage